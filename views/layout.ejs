<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <!-- Main Container that wraps everything -->
    <div class="container">
        <!-- Top bar containing logo, title, dashboard link, email, and buttons -->
        <header class="top-bar">
            <!-- Logo and Website Name Grouped -->
            <div class="logo-title">
                <a href="/">
                    <img src="/images/logo.png" alt="logo" class="logo-img">
                    <h1>WishJourney</h1>
                </a>
            </div>

            <!-- Conditional Display Area based on user login -->
            <% if (userId) { %>
            <div class="user-info">
                <p class="user-email"><%= user.email %></p>
                <a href="/dashboard" class="dashboard-link">MyDashboard</a>

                <form action="/logout" method="post" class="logout-form">
                    <input type="hidden" name="_method" value="delete">
                    <button>Logout</button>
                </form>
            </div>

            <% } else { %>
            <div class="login-section">
                <form action="/login" method="get">
                    <button>Login</button>
                </form>
            </div>
            <% } %>
        </header>

        <!-- Main Section -->
        <main>
            <%-body%>
        </main>

        <!-- Footer Section -->
        <footer>
            &copy; 2023 WishJourney
        </footer>
    </div>
    
</body>

</html>